{
  "name": "ordore-full-stack",
  "version": "1.0.0",
  "main": "server.js",
  "scripts": {
    "start": "node backend/server.js",
"heroku-postbuild": "npm install --prefix frontend && npm install -g @angular/cli && npm run build --prefix frontend",
  "postinstall": "npm install --prefix frontend"

  },
"engines": {
  "node": ">=18.19.0",

  "npm": ">=9.5.0"
},
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.18.2",
    "jsonwebtoken": "^9.0.2",
    "mongoose": "^8.2.1",
    "multer": "^1.4.5-lts.1",
    "socket.io": "^4.7.5",
    "twilio": "^5.3.0"
  }
}